services:
  api:
    image: node:22-alpine
    working_dir: /app
    command: sh -c "if [ ! -d node_modules ]; then npm ci; fi && npm run build && npm run start"
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://${POSTGRES_USER:-smart_queue}:${POSTGRES_PASSWORD:-smart_queue}@postgres:5432/${POSTGRES_DB:-smart_queue}}
      JWT_ACCESS_TOKEN_SECRET: ${JWT_ACCESS_TOKEN_SECRET}
      JWT_REFRESH_TOKEN_SECRET: ${JWT_REFRESH_TOKEN_SECRET}
      JWT_ACCESS_TOKEN_EXPIRES_IN_SECONDS: ${JWT_ACCESS_TOKEN_EXPIRES_IN_SECONDS:-900}
      JWT_REFRESH_TOKEN_EXPIRES_IN_SECONDS: ${JWT_REFRESH_TOKEN_EXPIRES_IN_SECONDS:-2592000}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379}
    volumes:
      - ./:/app
      - /app/node_modules
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started

  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-smart_queue}
      POSTGRES_USER: ${POSTGRES_USER:-smart_queue}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-smart_queue}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-smart_queue} -d ${POSTGRES_DB:-smart_queue}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data: